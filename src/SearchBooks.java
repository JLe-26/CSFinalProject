import java.util.ArrayList;

public class SearchBooks {
    // Methods that allow the user to search for a book within a category of title, author, genre, or keyword
    // Makes use of OrganizeBooks methods that will first alphabetize the books and then traverse the arraylist of book objects to return a specific book
    public static ArrayList<Book> bookSearch(ArrayList<Book> books){
        // will be used in organizeBooks method for keyword (tag)
        ArrayList<Book> organizedByTag = new ArrayList<>();
        return organizedByTag;
    }
}
